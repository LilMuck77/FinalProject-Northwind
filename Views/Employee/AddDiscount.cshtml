@using Northwind.Models
@model Discount



<h2 class="mt-3"><i class="fa fa-plus-circle"></i> Add Discount</h2>

<form method="post" asp-action="AddDiscount">
  <div asp-validation-summary="All" style="color:darkgreen;">
    @* DO I NEED THE CODE BELOW TO ASSIGN NEW DICOUNT ID *@
    @* <input type="hidden" name="DiscountId" value="@Model.DiscountId" /> *@
    <div class="form-row">
      <div class="form-group col-md-5">
        <label asp-for="@Model.Title">Enter Title</label>
        <input asp-for="@Model.Title" class="form-control" placeholder="Title" />
      </div>
      <div class="form-group col-md-5">
        <label asp-for="@Model.Description">Enter Description</label>
        <input asp-for="@Model.Description" class="form-control" placeholder="Description" />
      </div>
    </div>
    @* FORMAT DATE TO JUST DATE AND NO TIME *@
    <div class="form-row">
      <div class="form-group col-md-5">
        <label asp-for="@Model.StartTime">Enter Start Date</label>
        <input asp-for="@Model.StartTime" class="form-control" placeholder="Start Time" />
      </div>
      <div class="form-group col-md-5">
        <label asp-for="@Model.EndTime">Enter End Date</label>
        <input asp-for="@Model.EndTime" class="form-control" placeholder="End Time" />
      </div>
    </div>
    @* ADD RANDOM GENERATOR HERE AND PLACE INTO PLACEHOLDER FOR CODE *@
    <div class="form-row">
      <div class="form-group col-md-4">
        <label asp-for="@Model.Code">Discount's code</label>
        <input asp-for="@Model.Code" class="form-control" placeholder="Code" />
      </div>
      <div class="form-group col-md-3">
        <label asp-for="@Model.ProductId">Enter Product ID</label>
        <input asp-for="@Model.ProductId" class="form-control" placeholder="Prodcut ID" />
      </div>
      <div class="form-group col-md-3">
        <label asp-for="@Model.DiscountPercent">Enter Discount Percent</label>
        <input asp-for="@Model.DiscountPercent" class="form-control" placeholder="Discount Percent" />
      </div>
    </div>
  </div>



  <button type="submit" class="btn btn-outline-primary">Create</button>
  @* need to add random generator for coded, should we add discoount percent *@
</form>

